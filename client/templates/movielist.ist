@define "video-movie"
	.movie.inline-box[data-id={{ _id }}][data-length={{ length }}][data-fulltitle={{ title }}]
		.cover
			img[src=/rest/covers/video:thumb-1:{{ _id }}]
		.title "{{ title }}"

.movielist
	@each movies
		@use "video-movie"